builder


module.exports = {
    run: function(creep) {
        if(creep.memory.role == 'scout') {
            var travelTo = []; // outside of this room
            var 
            
            return;
        }
        if(creep.memory.role == 'builder') {
            creep.memory.;

        }
        if(creep.memory.role == 'harvester') {
            creep.memory.

        }
        if(creep.memory.role == 'upgrader') {
            creep.memory.

        }
        if(creep.memory.role == 'cleanup') {
            creep.memory.

        }
        if(creep.memory.role == 'digger') {
            creep.memory.
        }
        if(creep.memory.role == 'scout') {

        }
        if(creep.memory.role == 'spawner') {

        }
        if(creep.memory.role == 'tower') {
            
        }
 //       if(creep.memory.role == ' ') {
 //           
 //       }

    }
}